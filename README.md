For Review of concept of Inheritance check Inherit.cpp and Multiple_inherit.cpp
For Constructor method check constructor_method and then move to parameterized-constructor.cpp and finally to shallowcopy.cpp (this contains concept of both shallow as well as deepcopy contructor)
setter and getter are for applying methods to set and access the attributes of any class in  private access modifiers.
shortcut in Vscode to run any file: g++ "filename".vpp -o a.exe; if ($?) {.\a.exe} replace "filename"=Inherit for example.
